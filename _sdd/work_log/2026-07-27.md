# 2026-07-27

## 1 원격 재연결 상태 초기화 회귀 조사

- 무엇/왜: PR #2·#3의 파일 열기·디렉토리 펼치기 자동 재연결 뒤 기존 트리와 활성 파일이 초기화되는 회귀의 실행 경로를 추적했다.
- 결과: `connectRemoteWorkspace`가 재연결에도 기본 `reset` 인덱스 모드를 사용해 `fileTree`, `expandedDirectories`, `activeFile`을 비우는 것이 직접 원인이며, 원격 초기 인덱스가 루트 전용이어서 기존 펼침 하위 트리의 별도 재동기화도 필요함을 확인했다.
- 포인터: `src/workspace/use-workspace-remote.ts`, `src/workspace/workspace-context.tsx`, `electron/remote-agent/runtime/workspace-ops.ts`

## 2 원격 재연결 세션 보존 구현

- 무엇/왜: 파일 또는 디렉토리 사용자 동작이 끊긴 원격 연결을 복구할 때 현재 파일과 펼친 트리를 유지하면서 단절 중 변경된 하위 항목을 최신 상태로 맞추도록 구현했다.
- 결과: 전용 `reconnect` 인덱스 모드가 세션을 reconcile하고 펼친 디렉토리를 깊이순으로 재조회한 뒤 사용자 동작을 계속한다. 같은 디렉토리가 재연결 hydration에서 이미 성공적으로 로드된 경우 후속 조회를 생략한다. 실제 루트 전용 원격 인덱스를 모델링한 파일·디렉토리 회귀 테스트를 추가했다.
- 포인터: `src/workspace/workspace-context-types.ts`, `src/workspace/workspace-context.tsx`, `src/workspace/use-workspace-remote.ts`, `src/workspace/use-workspace-snapshot.ts`, `src/App.test.tsx`

## 3 구현 리뷰 수정 및 검증

- 무엇/왜: correctness와 simplicity 관점의 구현 리뷰를 실행하고 High·Medium 지적을 모두 반영했다.
- 결과: 재연결 시 펼친 전체 하위 트리가 stale해지는 문제, 비현실적인 eager-tree 테스트, 중복 타입·옵션 계층·미사용 hydration 계산을 수정했다. `npm test` 80개 파일에서 945개 통과(기존 skip 1개), `npm run lint`, `npx tsc --noEmit`이 모두 통과했다.
- 포인터: `src/App.test.tsx`, `src/workspace/workspace-context.tsx`, `src/workspace/use-workspace-snapshot.ts`

## 4 구현 후 스펙 동기화

- 무엇/왜: reconnect를 full-index reset으로 설명하던 canonical 계약을 구현된 세션 보존·expanded directory 재동기화 동작과 맞췄다.
- 결과: 최초 connect의 reset, 명시적 refresh의 비동기 hydration, user-action reconnect의 awaited hydration 및 triggering directory 단일-load 계약을 반영했다. BUG-05/BUG-06와 운영 검증 항목도 같은 동작을 가리키도록 갱신했으며 deferred/open question은 없다.
- 포인터: `_sdd/spec/workspace-and-file-tree/contracts.md`, `_sdd/spec/workspace-and-file-tree/overview.md`, `_sdd/spec/remote-workspace/overview.md`, `_sdd/spec/operations.md`, `_sdd/spec/feature-index.md`
