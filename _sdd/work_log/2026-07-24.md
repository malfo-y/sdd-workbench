# 2026-07-24 Work Log

## 16:12 Remote 파일 선택 시 자동 재접속 구현

- **무엇/왜**: 원격 워크스페이스가 `disconnected`인 상태에서 파일 브라우저의 파일을 선택하면 저장된 프로필로 자동 재접속하고, 연결이 복구된 경우 선택한 파일을 한 번 여는 동작을 구현했다.
- **결과**: 재접속 성공 시 파일 read를 정확히 한 번 수행하고, read 실패 시 연결은 유지한 채 기존 파일 오류를 표시한다. 재접속 실패로 실제 상태가 `disconnected`이면 read하지 않는다. SSH 연결 뒤 watcher/index 초기화가 실패해 helper가 `false`를 반환하더라도 실제 renderer 상태가 `connected` 또는 `degraded`이면 read를 진행하도록 경계를 보완했다. RED 테스트를 먼저 확인한 뒤 관련 사용자 동작 테스트 4개와 앱 통합 테스트 163개가 통과했다.
- **포인터**: [파일 선택 흐름](../../src/workspace/workspace-context.tsx), [회귀 테스트](../../src/App.test.tsx)

## 16:12 Remote 파일 선택 재접속 계약 동기화 및 검증

- **무엇/왜**: 구현된 자동 재접속·단일 read·실제 renderer 연결 상태 우선 규칙을 F01/F27 계약과 운영 검증 기준의 현재 사실로 동기화했다.
- **결과**: BUG-05를 `Fixed`로 기록하고 reconnect helper 반환값과 실제 연결 상태가 다른 경계 계약을 명시했다. 전체 회귀 935개가 통과했고 기존 skip 1개만 남았으며, lint·TypeScript·`git diff --check`도 모두 통과했다. 재접속 timeout 배너 assertion은 기존 표준 사용자 메시지 계약에 맞췄다.
- **포인터**: [Remote overview](../spec/remote-workspace/overview.md), [Remote IPC](../spec/remote-workspace/contracts.md), [Workspace overview](../spec/workspace-and-file-tree/overview.md), [Workspace 계약](../spec/workspace-and-file-tree/contracts.md), [운영 기준](../spec/operations.md), [기능 인덱스](../spec/feature-index.md)

## 16:31 Remote 파일 선택 재접속 리뷰 finding 수정

- **무엇/왜**: implementation review에서 발견한 High 경쟁 조건 1건, Medium 중복 2건, Low 테스트 누락 1건을 모두 닫았다.
- **결과**: reconnect helper가 `true`를 반환해도 setup 중 실제 session이 다시 `disconnected`가 되면 read하지 않도록 재시도 완료 시점의 renderer 상태를 항상 검사한다. helper가 `false`이고 실제 상태가 `degraded`인 경우의 1회 read 테스트를 추가했으며, 공통 원격 연결 UI 준비 절차를 fixture로 통합했다. 상세 reconnect 규칙은 workspace core 계약 한 곳에 두고 overview와 operations는 링크·검증 증거만 유지한다. High 경로는 read가 잘못 1회 호출되는 RED를 확인한 뒤 GREEN으로 전환했고, 전체 회귀 937개가 통과했으며 기존 skip 1개만 남았다. lint·TypeScript·`git diff --check`도 통과했다. 초기 스펙 구조 검사가 운영 테스트 증거까지 금지한 가정은 잘못이어서, 규범적 상세 중복만 금지하도록 검사 계약을 바로잡았다.
- **포인터**: [파일 선택 흐름](../../src/workspace/workspace-context.tsx), [회귀 테스트와 fixture](../../src/App.test.tsx), [Workspace 계약](../spec/workspace-and-file-tree/contracts.md), [Remote overview](../spec/remote-workspace/overview.md), [운영 기준](../spec/operations.md)

## 17:05 Remote 디렉토리 확장 시 자동 재접속 구현

- **무엇/왜**: 원격 세션이 `disconnected`인 상태에서 파일 브라우저의 lazy 디렉토리를 펼치면 즉시 실패하던 문제를 수정했다. 파일 선택과 동일한 사용자 액션 연결 guard를 디렉토리 확장에도 적용하되 watcher의 내부 hydration은 자동 재접속 대상에서 제외했다.
- **결과**: RED 단계에서 디렉토리 클릭 후 reconnect 호출이 1회에 머무는 실패를 확인했다. 재접속 성공 시 full-index reset으로 사라지는 클릭 대상의 확장 상태를 복원한 뒤 `workspace:indexDirectory`를 정확히 한 번 호출하고, 재접속 실패 후 실제 상태가 `disconnected`이면 자식 로드를 호출하지 않는다. 공통 guard의 첫 async 구현이 local/connected direct path에 microtask를 추가해 기존 파일 선택 순서를 깨뜨리는 회귀도 포착했으며, 사용 가능한 연결은 동기 fast path를 유지하도록 수정했다. 전체 회귀 939개가 통과했고 기존 skip 1개만 남았으며, lint·TypeScript·`git diff --check`도 모두 통과했다.
- **포인터**: [사용자 액션 연결 guard](../../src/workspace/workspace-context.tsx), [디렉토리 확장 흐름](../../src/workspace/use-workspace-snapshot.ts), [회귀 테스트](../../src/App.test.tsx), [Workspace 계약](../spec/workspace-and-file-tree/contracts.md), [Remote overview](../spec/remote-workspace/overview.md), [운영 기준](../spec/operations.md), [기능 인덱스](../spec/feature-index.md)

## 17:10 Remote 디렉토리 재접속 implementation review

- **무엇/왜**: disconnected lazy 디렉토리 자동 재접속 구현을 correctness와 simplicity 두 관점에서 독립 검토했다.
- **결과**: Critical·보안 결함은 없었고, `connecting`/`null` 상태를 direct path로 허용하는 High 1건, reconnect 중 최신 collapse 의도를 덮어쓰는 Medium 1건, direct path 확장 상태 중복 기록과 혼합 동기/비동기 판정식 과잉압축 Medium 2건을 확인했다. 단일 성공·실패 경로, watcher hydration 분리, 기본 회귀와 스펙 동기화는 충족으로 판정했다.
- **포인터**: [사용자 액션 연결 guard](../../src/workspace/workspace-context.tsx), [디렉토리 확장 흐름](../../src/workspace/use-workspace-snapshot.ts), [파일 트리 토글](../../src/file-tree/file-tree-panel.tsx)

## 17:23 Remote 디렉토리 재접속 review finding 수정

- **무엇/왜**: implementation review의 High 1건과 Medium 3건을 모두 닫았다.
- **결과**: workspace별 진행 중 reconnect Promise를 공유해 `connecting` 동안 추가 파일·디렉토리 액션이 세션 준비 전 IPC를 호출하지 않도록 했고, local 및 `connected`/`degraded`만 direct path로 허용했다. 파일 트리는 확장 의도를 lazy load보다 먼저 기록하며, reconnect 완료 시 최신 intent가 여전히 expand인 경우에만 경로를 복원하고 자식을 로드한다. expand→collapse 경쟁은 stale continuation이 `aria-expanded=true`로 되돌리던 RED, connecting 추가 액션은 `readFile`이 조기에 1회 호출되던 RED를 각각 확인한 뒤 GREEN으로 전환했다. direct path의 중복 확장 상태 write를 제거하고 혼합 판정식을 풀어 썼다. reconnect 회귀 10개, 파일 트리 50개, App 169개가 통과했고 기존 skip 1개만 남았으며, 전체 회귀는 941개 통과·기존 skip 1개였다. lint·TypeScript·`git diff --check`도 통과했다.
- **포인터**: [사용자 액션 연결 guard와 intent 추적](../../src/workspace/workspace-context.tsx), [reconnect 이후 확장 흐름](../../src/workspace/use-workspace-snapshot.ts), [파일 트리 토글 순서](../../src/file-tree/file-tree-panel.tsx), [경쟁 조건 회귀 테스트](../../src/App.test.tsx), [Workspace 계약](../spec/workspace-and-file-tree/contracts.md), [운영 기준](../spec/operations.md)
