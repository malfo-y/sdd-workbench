# LLM Comment Bundle

## Instruction
이거 아이디어 어떤지, 구현 난이도는 어떻게 되는지 알려 줘.

## Constraints
- Do not edit files not referenced by comments unless required to satisfy dependencies.
- Keep changes minimal and explain tradeoffs when assumptions are needed.
- Respect existing workspace boundaries and relative paths exactly as provided.

## Global Comments
지금 View comments와 Export comments가 분리되어 있는데, 사실 UX 관점에서 보면 코멘트를 일단 보고, export 할 코멘트를 골라서 export 하는 게 가장 좋음. 

그래서 view comment 하고 각 코멘트 왼쪽에 선택 버튼을 두고, 아래에는 Export Selected 같은 버튼을 둬서 이걸 누르면 Export Comment 창이 뜨게 하는 게 좋을 것 같다.
기본적으로 코멘트 왼쪽의 export 선택 버튼은 모두 체크되어 있게 하면 기본 모든 코멘트 export가 가능하다.

## Comments
### _sdd/spec/sdd-workbench/04-interfaces.md:L96-L96

view comment에서 export comment도 같이 하고 싶다.

- anchor.hash: 6bddc95f
- anchor.snippet: 2. `View Comments`는 상단 global comments(read-only) + 하단 line comments 목록을 함께 보여준다.
- anchor.before: 1. `Add Comment`는 CodeViewer/SpecViewer 모두에서 동일 저장 플로우를 사용한다.
- anchor.after: 3. `View Comments` 편집/삭제/Delete Exported는 동일 comments 저장 플로우를 재사용한다.
- createdAt: 2026-02-23T09:55:59.474Z
